name: Render Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *' 
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake Up Render
        run: curl -sL ${{ secrets.API_URL }}/api/health/heartbeat
